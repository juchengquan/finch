# iOS/macOS plans — web-drift checklist (working doc)

> **Transient working artifact** — delete once drift is reconciled. Generated by fact-checking the
> foundation specs against the live web code at **commit `22c9896`** (= `feat/frontend` + the
> `accountGroup` dead-code cleanup), **SCHEMA_VERSION `2026-06-14T00:00:00Z`**, on 2026-06-13.
>
> Scope of this pass: `IOS_MACOS_PLAN.md`, `IOS_MACOS_WIRE_FORMAT.md`, `IOS_MACOS_PHASE_1_0_PLAN.md`,
> `IOS_MACOS_PHASE_1_DESIGN.md`, `IOS_MACOS_PHASE_1_5_DESIGN.md`, `IOS_MACOS_PHASE_1_5_PLAN.md`,
> `IOS_MACOS_PHASE_2_DESIGN.md`, `IOS_MACOS_PHASE_2_PLAN.md`.
>
> Mark each: **[x] fix** · **[k] keep (intentional)** · **[?] discuss**. `[FIX]` = clear drift (recommend fix).
> `[RULING]` = could be an intentional native choice (your call).

---

## What's CORRECT (no action) — the load-bearing parts

- ✅ **The 74 chokepoint actions** — full action list + count verified by `comm` diff vs `_args.ts`, zero drift both directions. `ActionName` enum matches 1:1 (+`setEntryAttachment` correctly tagged the 75th, Phase 6.5).
- ✅ **Per-action Args shapes** — 12+ spot-checked field-for-field against `_args.ts`/per-domain `types.ts`; no renamed/added/removed fields.
- ✅ **Pack format** — `finch.sqlite3`(DEFLATE) + `attachments/`(STORE) + `manifest.json`, `PACK_FORMAT_VERSION='1'`, level 6, magic-byte detect — byte-for-byte (`pack.ts`).
- ✅ **I18nError wire shape** `{code, params, message}` (`lib/i18n-error.ts`).
- ✅ `mutate.ts` dispatcher = 53 lines; `LedgerRow.base` (not `baseCurrency`); FTS5 indexes `description`+`notes` only; SCHEMA_VERSION const = `2026-06-14T00:00:00Z`; anomaly threshold `2.5` / minCount `3`; per-domain action **names** + counts; `removeBudget`/`setRuleEnabled` phantom notes; seeded ledgers personal/family/business/travel = USD/SGD/CNY/JPY.

---

## TIER 1 — Load-bearing falsehoods (a porter builds the WRONG thing) · all [FIX]

- [ ] **1. Audit problem classes: plan says 8, code emits 10 — and the names are fabricated.**
  `Phase 1.0 PLAN` Task 3 / `Phase 1 DESIGN` §4, §8.2.
  Plan list (`balanceImbalance, missingAccountLeg, missingCategoryLeg, missingCounterparty, duplicatePosting, orphanAttachment, brokenTransfer, pinnedRateOutOfDate`) — **none exist**.
  Code (`entries.ts:709`): `unsealed, unbalanced, too-few-legs, no-account-leg, currency-mismatch, cross-ledger, base-identity, kind-shape, trial-balance, balance-drift`. → Replace count + entire list; reconcile DESIGN (closer) and PLAN.

- [ ] **2. The "2026-06-14 cutover migration" story is dead code.**
  `PLAN` §2 table L123, §4.6 L744-763, §17 L1327.
  Plan: data-move "replayed inside the MIGRATIONS entry stamped 2026-06-14"; `MIGRATIONS`+`applyMigrations` in `entries.ts`; `lib/db/cutover.ts` is the builder.
  Code: cutover removed as dead code (`schema.ts:604-616` comment — canonical SCHEMA already post-cutover); `MIGRATIONS` + runner `migrate()` live in `core/schema.ts` (441, 688); **no `applyMigrations`**, **no `cutover.ts`**; the only DE migration entry is `2026-06-13` (table-creation, no data move). SCHEMA_VERSION const stays `2026-06-14`. → Rewrite the migration narrative; drop the `cutover.ts` row; `applyMigrations`→`migrate`; file `entries.ts`→`core/schema.ts`.

- [ ] **3. Schema is ONE blob, not per-table DDL constants.**
  `Phase 1.0 PLAN` Task 2 Step 1/4. Plan: "port each `ENTRIES_DDL`/`POSTINGS_DDL`/`*_DDL` from `schema.ts` lines 33-200".
  Code: whole schema is one `export const SCHEMA` template literal (`schema.ts:56`); only split constants are `ENTRY_ATTACHMENTS_DDL` (:18) + `ENTRIES_FTS_DDL` (:37). `entries`/`postings` DDL live in **`core/entries-schema.ts`** (8, 36), not `schema.ts`. → Rewrite the port instruction.

- [ ] **4. Version is read from a table, not a PRAGMA; MIGRATIONS has many entries.**
  `Phase 1.0 PLAN` Task 2 Step 5/7. Plan: `PRAGMA user_version`; "MIGRATIONS has only one entry — the cutover".
  Code: `db_metadata.schema_version` table (`schema.ts:344`, `ensureMetadataRow` :665); `MIGRATIONS` is a keyed record iterated sorted — multiple versioned entries. → Fix both.

- [ ] **5. Four selector signatures (and one algorithm) are wrong.** `Phase 1.5 DESIGN` §6 / `PLAN`.
  - `monthForecast`: plan `(txns, accounts, ledgerId, n)→[MonthForecast]`, "linear regression / 90 days / multi-month". Code (`select.ts:279`): `(txns, scheduled, ledgerId, month, today)→MonthForecast|null`; **MTD + daily run-rate × days-remaining + upcoming scheduled**; single month; no `accounts`.
  - `unrealizedFx`: DESIGN `(txns, accounts, ledgerId)→[]`; PLAN `(holdings, exchangeRates, ledgerBase)→Decimal`. Code (`:906`): `(account, txns, toBase)→number`, per-account.
  - `suggestCategory`: DESIGN `(tx, options:[Category])→id`; PLAN `(tx, merchantStats)`. Code (`:842`): `(txns, ledgerId, description, counterpartyId?, opts)→CategorySuggestion|null` (minCount 1, minConfidence 0.5).
  - `netWorthByMonth`: plan drops `ledgerId`. Code (`:166`): `(txns, accounts, ledgerId, endMonth, n, toBase?)`.
  → Correct all four; reconcile the DESIGN-vs-PLAN disagreements.

- [ ] **6. Two fictional "DB exceptions" — all selectors are pure.** `Phase 1.5 DESIGN` §3.
  Plan: `monthForecast` reads `RateSnapshot` for FX; `weeklyDigest` reads `app_state` for a configurable first-day-of-week → used to justify `Selectors.DBContext` / a GRDB reader.
  Code: both pure; `weeklyDigest` hardcodes ISO Monday (`select.ts:604-606`); `monthForecast` no DB. §6 even says "all 25 are pure" — self-contradiction. → Remove both claims; reconsider whether `DBContext`/`throws` machinery is needed at all.

- [ ] **7. The "7 Phase-1.0 selectors" — PLAN and DESIGN name DIFFERENT sets.**
  PLAN: `balanceSeries, netWorthSeries, selectTransactions, categorySpend, budgetProgress, merchantStats, anomalyScore`.
  DESIGN: `accountBalance, selectTransactions, categorySpend, budgetProgress, cycleWindow, merchantStats, anomalyScore`.
  All exist, but they can't both be "the 7." → Pick one canonical set.

---

## TIER 2 — Stale counts (mostly from the just-landed cleanup commit) · all [FIX]

- [ ] **8. "77 entries / 3 accountGroup duplicates" → 74 entries / 74 unique / 0 duplicates.**
  `WIRE_FORMAT` §2.2 L188; `PLAN` §2 L124; `Phase 2 DESIGN` §2 L175/§12. Removed by commit `22c9896`. (Phase 2 prose already says cleanup "has landed" — header just contradicts it.)
- [ ] **9. `entries.ts` "12-export module" → 13 callable exports.** `Phase 2 DESIGN` §2 L120/§4; `PLAN` §2 L130. The plan's own §4.1 table lists all 13.
- [ ] **10. "13 per-domain mutations.ts of 14 domains, attachments queries-only" is wrong twice.**
  Reality: **12** first-class domains have a `mutations.ts`; the 13th merged map is **`_app/`** (escape hatch). `budgetGroups` ALSO has none (its 3 group actions live in `budgets/mutations.ts`). `Phase 2 DESIGN` §2 L50/L120.
- [ ] **11. "5 cross-domain deps" → 4.** `accounts → accountGroups` removed by the cleanup; survivors: `transactions→attachments`, `budgets→budgetGroups`, `rules→counterparties`, `scheduled→counterparties`. `Phase 2 DESIGN` §2/§6. (Side note: web `AGENTS.md` L19 also still lists 5 — separate web-doc fix.)
- [ ] **12. `accounts/mutations.ts` 73 → 51 lines; per-domain port total 1,335 → 1,313.** `Phase 2 DESIGN` §2 table/§1 L91.
- [ ] **13. "32 selectors" headline.** Code = 33 top-level exports (32 selectors + `kindOf` helper; plus `currentMonth`/`prevMonth`/`cycleWindow` date helpers). The "32" is a curated count; the 7+25 breakdown is internally inconsistent and double-lists several. `Phase 1.5 DESIGN` §2; `PLAN` Task 1; `WIRE_FORMAT` §5; `PLAN` §2 L130. → Restate as a consistent partition (decide the canonical figure).

---

## TIER 3 — Renamed symbols / paths / line numbers (mechanical) · all [FIX]

- [ ] **14.** `PLAN` §2 source table: `lib/db/schema.ts`→`core/schema.ts`, `lib/db/entries.ts`→`core/entries.ts`, `lib/db/paths.ts`→`core/paths.ts` (the changelog already uses `core/`; §2 is the outlier).
- [ ] **15.** `recomputeAccount` → `recomputeAccountFromPostings` (`entries.ts:507`). `PLAN` §2.3/§4.6.
- [ ] **16.** `merchant-picker-sheet.tsx` → `merchant-picker-dialog.tsx`; `rule-builder-sheet.tsx` → `rule-builder-dialog.tsx`. `PLAN` §3 rows 13, 31.
- [ ] **17.** `idx_attach_entry`/`idx_attach_ledger` → `idx_eattach_entry`/`idx_eattach_ledger`. `PLAN` §2.5.1.
- [ ] **18.** `refund-badge.tsx` → `components/ui/refund-badge.tsx`. `PLAN` §3 row 9.
- [ ] **19.** `Tx.account` = "account name" → **account id** (`state.ts:71`). `Phase 1 DESIGN` §7 (PLAN already correct).
- [ ] **20.** `anomalyScore(tx, stats, threshold: Double = 2.5)` → `anomalyScore(tx, stats, opts:{minCount?, threshold?})` (2.5 default itself is right). `Phase 1.0` Task 6; `Phase 1.5`.
- [ ] **21.** Line-cite fixes: `r2` `entries.ts` L24→**L18**; rollover coercion `budgets/mutations.ts` L88/`Number()`→**L50** truthy `?1:0`; displayCurrency setter `ledgers/mutations.ts:58-64`→**`_app/mutations.ts:35`** (58-64 is `deleteLedger`).
- [ ] **22.** `lib/db/seed.ts` → `lib/db/core/seed.ts`. `Phase 1.5` §4.2.
- [ ] **23.** Pack manifest shape in `PLAN` §2.5.3: flat `db_sha256`/`row_counts`/`attachment_count` → nested `db:{sha256,row_counts,byte_size}` + `attachments:{}` (`pack.ts:43-60`). WIRE_FORMAT already correct — align PLAN to it.
- [x] **24. WITHDRAWN — `<entry_id>` was already correct.** The real upload code (`app/api/attachments/route.ts`) builds `attachments/<entry_id>/<att_id>.<ext>` from `entryId`; the `pack.ts:6` comment saying `<transaction_id>` is the stale one (pre-DE leftover). No change needed; an erroneous edit to `Phase 1 DESIGN` was reverted.
- [ ] **25.** i18n "~30 codes total in `lib/i18n-error.ts`" → codes live in **per-domain `errors.ts`** (14 distinct constants / 65 `error.*` strings); `i18n-error.ts` is the class only. `Phase 2 DESIGN` §9.4.

---

## TIER 4 — References to web artifacts that don't exist · [FIX]/[?]

- [ ] **26. [FIX]** `scripts/export-fixtures.ts` referenced as **existing** in `Phase 1.5` §4.1 & `WIRE_FORMAT` §5 (Phase 1.0 correctly frames it as a *Create* task). → Reword present-tense refs to "to be created in Phase 1.0."
- [ ] **27. [?] DESIGN GAP, not a typo.** The fixture-export harness assumes a `CASES` array / `extractTestCases('select.test.ts')`. Real `lib/select.test.ts` is **flat `test(...)` calls with inline literals** — no structured cases to extract; `lib/db/core/seed.ts` is the seed. The harness **as designed won't run**. → Needs a real rework (export a `CASES` table from the web test, or change the extraction strategy). `Phase 1.5` §4.2 / PLAN Task 3.

---

## TIER 5 — Genuine product/design rulings (NOT web drift — your call) · [RULING]

- [ ] **28. Insights tab composition.** Plan wires **Holdings + Unrealized-FX** (and `recentExpenses`, `netWorthSeries`) cards into the **native Insights tab**. In the web these selectors feed **Account Detail**, not Insights. → Intentional native enhancement (keep), or mis-citation to correct (move to Account Detail)? `Phase 1.5 DESIGN` §5.2.
- [ ] **29. "Force import" UI.** A native-only safety hatch in Settings › Advanced; the web has **no** such UI (its import is a plain file-picker). → Keep as a native feature but stop implying the web has it. `Phase 2` §10 / `Phase 1.0`.

---

## Cross-cutting note
Many TIER-1/3 items are **DESIGN-vs-PLAN internal contradictions** (the two files for a phase disagree with each other, and the code breaks the tie). Fixing against code resolves both sides at once.
