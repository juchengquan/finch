# ios/CLAUDE.md

Guidance for the **native port of finch** — the SwiftUI app under `ios/`, shared by
iOS, macOS (`FinchMac`), watchOS, the Widget, and the Share extension. This is where
the active work happens; the root `CLAUDE.md` documents the web `frontend/`.

Design docs live in `plans/ios-macos/` — start at `IOS_MACOS_INDEX.md`, then
`IOS_MACOS_UI_GAP_AUDIT.md` (capability/gap matrix) + `IOS_MACOS_UI_REMEDIATION_PLAN.md`
(the prioritized build-out). The native app may now add features *ahead* of the web and
feed them back to `frontend/`.

## Layout

- **`FinchCore/`** — the SwiftPM package: the read-side engine + the write chokepoint. A
  Swift port of the web's `frontend/lib/db/`, kept at behavioral **parity** (see below).
  Consumed by every app target. Sources in `FinchCore/Sources/FinchCore/`, tests in
  `FinchCore/Tests/` (`FinchCoreTests` + `ParityTests`).
- **`FinchApp/Sources/FinchApp/`** — the SwiftUI app: the `FinchStore` state shell + all
  screens. A thin UI/state layer over `FinchCore`.
- **`Shared/`** — `WatchSnapshotPayload*` (Foundation-only; compiled into both the phone
  app and the Watch app so the Watch needs no `FinchCore`).
- **`FinchWidget/`, `FinchWatch/`, `FinchWatchComplication/`, `FinchShare/`** — the
  extensions and Watch app.
- **`scripts/`** — localization tooling (Bun/TypeScript). **`docs/`** — `simulator-ui-driving.md`.
- **`swiftui-vs-uikit.md`** — the SwiftUI-first rule (read it before reaching for UIKit).

## Build & run

> The Xcode project **`FinchApp.xcodeproj` is generated by XcodeGen and git-ignored**. Run
> `xcodegen generate` before opening/building, and again after adding any file under
> `FinchApp/Sources/` — new files only enter the project on a regen. Editors that respect
> `.gitignore` (VS Code) hide the `.xcodeproj` from the tree.

- **`swift build` / `swift test` build only `FinchCore`** (the SwiftPM package rooted at
  `ios/Package.swift` — there is **no** `ios/FinchCore/Package.swift`). The app targets are
  built through the generated Xcode project, **not** SwiftPM.
- **CLI:** if `xcode-select -p` points at `CommandLineTools`, prefix `xcodebuild` with
  `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`.
- **iOS → Simulator:** scheme `FinchApp`, an iPhone destination. Bundle id
  `com.juchengquan.finch`. There's an **`ios-build-launch` skill** that builds/installs/
  launches on a sim — prefer it.
- **macOS → native window (NO simulator):** scheme `FinchMac`, "My Mac". It signs **ad-hoc**
  (`CODE_SIGN_IDENTITY = -`, no Development Team). To make team-less signing work its **App
  Group was dropped** from `FinchApp/FinchMac.entitlements` (so there is no Mac widget;
  `AppGroup.containerURL` falls back to Application Support).
- **CI** builds the simulator **unsigned** (`CODE_SIGNING_ALLOWED: NO`) and does **not**
  build `FinchMac`. Keep it that way. Device builds need a local `DEVELOPMENT_TEAM` (don't commit it).

### Targets (`ios/project.yml`, XcodeGen)

`FinchApp` (iOS app), `FinchWidget` / `FinchShare` (iOS app-extensions), `FinchMac` (macOS
app, PRODUCT_NAME `finch`), `FinchWatch` (watchOS app), `FinchWatchComplication` (watchOS
extension), plus `FinchAppTests` / `FinchCoreTests`. `FinchMac` shares the **same SwiftUI
sources** as `FinchApp` + `Shared/`; iOS-only modifiers are shimmed behind `#if os(macOS)` in
`Common/PlatformCompat.swift`, and unguarded `Shared/` references must compile on macOS too.

> **Info.plist gotcha:** `FinchApp`/`Widget`/`Share`/`Watch` use hand-authored `INFOPLIST_FILE`
> with `GENERATE_INFOPLIST_FILE: NO`. Do **not** switch them to XcodeGen's `info:` block — it
> would regenerate the plist and wipe the `.finch` UTI / document-type / `finch://` URL-scheme
> keys. Edit `Info.plist` directly.

## Test

- **`cd ios && swift build && swift test`** runs `FinchCoreTests` + the four `ParityTests`
  gates. App-level tests (`FinchAppTests`) run via the Xcode `FinchApp` scheme.
- **Regenerate parity fixtures:** `cd frontend && bun scripts/export-fixtures.ts`. Two
  gotchas (also in `ios/README.md`):
  - After changing `WRITE_SEQUENCE`, keep only `writeparity/sequence.json`; the audit /
    projection / round-trip fixtures otherwise change by `datetime('now')` noise alone —
    **revert them**.
  - **No wall-clock-dated actions in `WRITE_SEQUENCE`** (`postScheduled`, `createAccount`
    with `openingBalance`) — their entries stamp "today", so the oracle passes the day it's
    generated and fails the next.

## Architecture

Two layers: the **`FinchCore` engine** (SwiftPM) and the **`FinchApp` SwiftUI shell**.

### FinchCore — the engine (parity port of `frontend/lib/db/`)

`FinchCore` is a behavioral mirror of the web DB layer, verified against fixtures the web
side generates (`frontend/scripts/export-fixtures.ts`). Modules under `FinchCore/Sources/FinchCore/`:

- **`Storage/`** — `Schema.swift` is the web's interpolated `SCHEMA` DDL copied **byte-for-byte**
  (double-entry `entries`/`postings`, two-phase `sealed` write, balance/currency-guard triggers,
  FTS5). `Schema.version` must equal the web `SCHEMA_VERSION` (currently `2026-07-17T00:00:00Z`).
  Also `Migrations.swift` (fresh-DB `DatabaseMigrator`, the standard init path via
  `Migrations.runAll`), `Pack.swift` (the `.finch` ZIP format — snake_case manifest via explicit
  `CodingKeys`, **never** `.convertFromSnakeCase`), `Audit.swift` (the 10-code ledger sweep,
  ported verbatim; `jsNum()` formats doubles like JS `Number.toString` so `detail` strings match
  byte-for-byte), `DownSync.swift` (CloudKit fresh-device seed; pure DB I/O, no CloudKit dep).
- **`Store/`** — the **write chokepoint** (mirror of web `lib/db/mutate.ts` + `domain/`).
  `ActionName.swift` is the action catalogue — the web's action set **plus a few native-only
  actions** (`setEntryAttachment`, and native-first `setBudgetOrder` / `setTrackedCurrencies`
  the web ignores). `Apply.apply` / `applyReturningId` dispatch to per-domain `handlers` maps
  in `Store/Domain/*.swift`. `JSONValue`/`Args` are the arg/patch value types.
- **`Project/`** — the read model (mirror of `projectState`). `Projection.run()` produces
  `[Tx]` at **per-account-leg-posting grain** (one entry can yield several `Tx`; opening entries
  excluded), enriched with category/splits/tags/transfer/refund. `Money.swift` is the currency
  core (see below). `WidgetSnapshot.swift` also defines `enum AppGroup`
  (`group.com.juchengquan.finch`).
- **`Selectors/`** — pure read-model selectors (`accountBalance`, `categorySpend`, `netWorth`,
  budget/forecast/insights/holdings/reconcile). Compute figures from these; don't store them.
- **`Rules/`** (conditional rules engine) and **`Export/`** (CSV).

**Don't hand-edit** `Schema.ddl`, `Pack`, or `Audit` — they're generated/verbatim from the TS
source; re-emit from the web side. Parity is enforced by four gates in `ParityTests/`:
selectors, audit, projection, and a write round-trip that replays `WRITE_SEQUENCE` through the
Swift `Apply` chokepoint and compares an id-/timestamp-agnostic canonical snapshot.

### FinchStore — the app store

`FinchStore` (`@MainActor final class … : ObservableObject`, singleton `FinchStore.shared`) is
the app's zustand-equivalent. Like the web store it holds a **projection, not the source of
truth** — `@Published` slices (`txns`, `accounts`, `budgets`, …) for the **active ledger only**;
the SQLite DB is authoritative.

- **Mutation chokepoint:** every write → `store.apply(action, args)` / `applyReturningId(...)`
  → `FinchCore.Apply` (writes the DB, can reject with `I18nError`) → `reprojectActiveLedger()`
  re-reads slices → republish. This is the analogue of the web's `syncMutation` / `POST
  /api/mutate`. **No optimistic UI** — write-then-reproject, synchronously, all-or-nothing
  (keeps last-good state + sets `dataError` on failure). **Every feature — Siri, Watch,
  notification actions, seeds — funnels through this one chokepoint.** Nothing mutates the DB
  directly.
- **Write side-effects (fire on every `apply`):** Spotlight re-index, notification re-plan,
  widget-snapshot write + timeline reload, debounced auto-backup, CloudKit outbox note. Writes
  aren't cheap — bulk seed paths call `FinchCore.Apply.apply` directly to bypass them.
- **DB location:** `Application Support/finch.sqlite3`, one GRDB `DatabaseQueue` (never a
  `DatabasePool`). **Under XCTest it relocates to a temp dir** so import tests can't clobber the
  dev sim's data. The App Group is used only for cross-process scratch (widget snapshot, staged
  Share receipts), **not** the main DB.
- **`store.today` is data-anchored (`max(tx.date)`), not the wall clock** — used for budget
  windows / forecasts (demo determinism). Literal "today" surfaces (calendar, Today/Yesterday
  labels, next-run dates) must use **`store.wallToday`**. Mixing them is a real bug source.
- Per-device view prefs (active ledger, privacy mode, saved searches, appearance) live in
  **UserDefaults, never in the DB or exports** (iOS analogue of web localStorage).

### The adaptive shell & navigation

Unlike the web's single-render `PageShell`, the **chrome is two renders**: `Shell/AdaptiveShell.swift`
switches on `horizontalSizeClass` → `TabBarShell` (compact/iPhone) vs `SplitViewShell`
(regular/iPad/Mac, master-detail via `Shell/MasterDetailShell.swift`). But the **content views
are single-render "one view, two layouts"**: each takes an optional `selection: Binding<String?>?`
— non-nil ⇒ three-column selection mode, nil ⇒ compact push mode.

- **7 `AppTab`s ≠ 5 bottom slots.** `AppTab` (`ledger, accounts, activity, budgets, insights,
  scheduled, settings`) is defined in **`DeepLink/DeepLinkRouter.swift`**, not `Shell/`. The
  compact bottom bar shows only Accounts/Budgets/Scheduled/Insights/Settings; **Activity is
  hosted inside the Accounts tab** (not its own slot) and **Ledger is a top-left corner-push**
  (`LedgerBarButton`). Launch tab is `.accounts`.
- **Routing is centralized in `DeepLinkRouter`** (`@MainActor` `ObservableObject` `.shared`) so
  App Intents / notifications / Spotlight (outside the SwiftUI tree) drive the same state the UI
  observes. Internal routes use a `"<domain>:<id>"` string (also the Spotlight item id).
- **Command palette (⌘K):** `Shell/CommandPalette.swift` + `Shell/FinchCommands.swift` (macOS
  menu bar / iPad keys: ⌘N add, ⌘1–6 tabs, ⌘⇧E export, ⌘⇧H hide amounts). The palette and add
  sheets are presented at **app root**, and force-dismissed when the biometric lock engages.

### Write screens (`WriteScreens/`)

Standard sheet = `NavigationStack { Form }`, `.inline` title, toolbar with **icon buttons not
text**: `.cancellationAction` = ✕, `.confirmationAction` = ✓ (`confirmCheckmarkStyle()` in
`Common/ViewModifiers.swift`, accent-filled, auto-Liquid-Glass on iOS 26). Pickers are
tap-to-open full-height sheets: `SearchablePickerRow` (generic, stage-then-Confirm),
`CategoryPickerRow` (hierarchy tree), `MerchantPickerRow` (free-text, **commits on tap** — a
deliberate workaround because `.searchable` collapses the nav bar and hides Confirm),
`TagChipFlow`/`TagField` (multi-select chips via custom `FlowLayout`). `AddTransactionSheet` is
the flagship (expense/income/transfer/refund + opt-in adjust; amount is in the **account's own
currency**).

### Money & currency (easy to get wrong — mirror of the web gotcha)

**Amounts are stored in the ledger's BASE currency; display in a per-ledger DISPLAY currency.**
Don't format raw amounts, and **never call `Money.format` directly in a view** (it bypasses the
privacy mask). Use the privacy-aware helpers on `FinchStore` (`FinchStore+ViewHelpers.swift`):

- **`displayMoneyBase(_ baseAmount:)`** — a base-currency amount → convert to display + format.
- **`displayMoney(_ amount:, from currency:)`** — an account-currency amount → base → display.
- **`displayNative(_ amount:, currency:)`** — format in the given currency with **no conversion**
  (privacy-aware replacement for `Money.format`).
- **`toBase(_:from:)`** converts account-currency → base; aggregates (`netWorthDisplay`, etc.) sum
  in base then format to display. Per-ledger variants (`…forLedger:`) exist because the two-layer
  Ledger detail works for any ledger, not just the active one.

`baseCurrency` = active ledger's `.base`; `displayCurrency` = `displayCurrencyByLedger[activeLedgerId]`
(DB-backed via `app_state`, defaults to base until set in the Ledger screen). Core FX is
`FinchCore` `Money.swift`: **hub currency USD**, `ExchangeRate.rate` is **USD-per-1-unit**;
`Money.convert` returns **`nil` when a currency is unrated** and callers then pass the amount
through **unconverted** — so display-currency options are restricted to *rated* currencies.
Privacy mode masks every `display*` helper with `"••••"`.

### Sync & data (mostly inert scaffold — read carefully)

- **CloudKit sync (`Sync/CloudKitSync*.swift`, `SyncMutation.swift`) is scaffold, not shipping.**
  Loud `⚠️ UNVERIFIED AT RUNTIME` headers — built without a provisioned container, never
  two-device tested. Model: syncs **mutations, not rows** — each local `apply` becomes a
  `SyncMutation` pushed to a per-ledger CloudKit zone; other devices **replay through their own
  chokepoint**. Trust the pure/tested pieces (`SyncOutbox`, mappers, `CloudKitConflict`,
  `DownSync`); treat the transport as unproven. **CloudKit *traps* (not throws) when unentitled**,
  so `hasCloudKitEntitlement` pre-detection gates everything — never "simplify" that guard. Sync
  is inert until the container is provisioned (`plans/ios-macos/IOS_MACOS_PHASE_8_CLOUDKIT_SETUP.md`).
- **iCloud Drive pack sync (`Sync/ICloudSync.swift`)** — the older Phase-5 mechanism, still alive:
  pushes each auto-backup `.finch` to iCloud Drive; watches for newer packs and surfaces them for
  **manual** import (never silently swaps the live DB). Nil-safe when no ubiquity container.
- **`Sync/AutoBackupManager.swift`** — debounced (~5s) `.finch` snapshot to
  `Application Support/Backups/` (retention 14), then hands to `ICloudSync`.
- **`Sync/RateAutoUpdater.swift`** — the app's **only third-party network call**: daily ECB FX
  refresh from `api.frankfurter.dev` (no key, ~20h throttle, fails silently offline). Stores the
  **inverse** of the API quote (USD-per-unit).
- **Import/export (`FinchStore+ImportExport.swift`, `ImportExport/`)** — the `.finch` pack
  pipeline. **Import runs an AUDIT GATE pre-swap**: on `PackError.auditFailed` the live DB is
  untouched (a rejected pack is retained for the Face-ID-gated "Force import"). Export =
  `VACUUM INTO` a clone through GRDB + bundle attachments. Export/delete/force-import are
  **biometric-gated** (`Security/BiometricGate`, which **fails open** on a bare simulator).
- **Demo seed:** a fresh DB (no ledgers) gets `SimulatorDemoSeed.seed` (rich ~3-month dataset, incl.
  a second EUR "Travel" ledger) on **simulator** builds, else `seedMinimalStarter` (one Personal/USD
  ledger). Release keeps the minimal starter. (Note: account ids are a **global** PK, not per-ledger.)

### OS integrations

- **Deep link:** the only external route is **`finch://add`** (`+ ?account=<id>` to pre-select),
  handled in `DeepLink/DeepLinkRouter.handle`. Richer navigation is in-process router state, not URLs.
- **App Intents (`AppIntents/`):** 7 Siri/Shortcuts intents + 3 App Entities; every write goes
  through `store.apply`. **Biometric lock is enforced here too** — while locked, data actions refuse
  and entity queries return `[]` so Siri can't even enumerate account/category/ledger names.
- **Widgets + Watch — two snapshot types, two transports:** `WidgetSnapshot` (in `FinchCore`) is
  written to the **App Group file** and read by the read-only WidgetKit extension (same device).
  `WatchSnapshotPayload` (in `Shared/`, Foundation-only) is pushed to the Watch over **WCSession
  `updateApplicationContext`** because App Groups don't cross devices. The Watch carries its own
  quick-add catalog inside the payload (works with zero `FinchCore`); wrist adds land as **pending**
  on the phone.
- **Spotlight (`Spotlight/`):** full re-index (delete-all + re-add) on launch, after every write,
  and after import; **cleared while biometrically locked**, rebuilt on unlock.
- **Notifications (`Notifications/`):** 100% **local**, no push. Content computed at plan-time with
  stable ids so re-planning is idempotent. `NotificationPlanner` is the pure/tested decision core.
- **`Security/`:** optional biometric lock (`LAPolicy.deviceOwnerAuthentication`, device-passcode
  fallback; policies off/onLaunch/onBackground/onIdle; **fails open** if the device can't evaluate).
- **`PowerTools/`** is **reference-data admin UI** (Categories, Currencies/FX, Rules, Tags,
  Counterparties, statement import) — power-user management, *not* an OS-integration feature.

### i18n

Localization is **generated, not hand-edited.** English is the source; **the only translation
target is `zh-Hans`** (`FinchApp/Sources/FinchApp/Resources/Localizable.xcstrings` +
`AppShortcuts.xcstrings`). Untranslated keys fall back to English at runtime. Pipeline (Bun,
documented atop `scripts/build-xcstrings.ts`): `xcodebuild -exportLocalizations` → `xliff-keys.ts`
(→ `extracted-keys.json`, the authoritative key set) → `build-xcstrings.ts` rebuilds the catalog
with precedence **`zh-manual.json` > web-derived map (`frontend/messages/{en,zh-CN}.json`, aligned
positionally, `%`-keys excluded) > English fallback**. Re-run all three after adding UI strings;
hand edits belong in `scripts/zh-manual.json` (which pins terminology to the web's).

## Conventions

- **SwiftUI-first** (`ios/swiftui-vs-uikit.md`): build every screen in SwiftUI (~99% is). Drop to
  UIKit/AppKit only when SwiftUI genuinely can't express it, and bridge the **smallest** piece
  behind a `Representable` in `Common/`. Only 4 sanctioned UIKit/AppKit touches today
  (`Common/ActivityMonitor`, `FinchShare/ShareViewController`, `openSettingsURLString`,
  `NSMetadataQuery`). Deployment floor is deliberately wide: **iOS 17 / macOS 14 / watchOS 10**.
- **The encapsulation boundary is the module, not the type.** `FinchStore` members shared with its
  `+Sync` / `+ImportExport` / `+ViewHelpers` extensions are intentionally `internal`, not `private`
  — don't "tighten" them.
- **Driving the simulator** — `simctl` has no tap/swipe. **Use `idb`**: it drives the sim over its
  own companion socket and needs **no Accessibility permission**.
  `~/.local/idb-venv/bin/idb ui tap <x> <y> --udid <UDID>` (also `ui swipe`, `ui describe-all`).
  Installed via `brew install facebook/fb/idb-companion` + `fb-idb` in a **Python 3.11** venv —
  `fb-idb` calls `asyncio.get_event_loop()`, which raises on 3.12+, so a default-Python venv fails
  at startup. `ui describe-all` returns the accessibility tree with frames **in points** — use it to
  *measure* layout rather than eyeballing screenshots (this is how the #554 sheet-inset bug was
  found after several wrong guesses from pixel-squinting).
  **AppleScript/AX is a dead end in this setup:** under `tmux`, TCC attributes `osascript` to the
  detached tmux server (PPID 1), so granting Accessibility to the terminal *or* to ClaudeCode.app
  changes nothing — you'd have to grant the tmux binary and restart the server, killing the session.
  Fastest jumps use **DEBUG-only launch args** `-initialTab <tab>` and `-openAdd YES`
  (`FinchApp.swift init()`), or the `finch://add` deep link. Bundle id `com.juchengquan.finch`.
  (`ios/docs/simulator-ui-driving.md` has the longer recipe, but note `ios/docs/` is swallowed by the
  root `docs/` gitignore rule, so it is **local-only** and does not reach a fresh clone.)
